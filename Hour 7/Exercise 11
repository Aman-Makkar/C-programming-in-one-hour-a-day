#include <stdio.h>

int main (void)
{
	int count, temp;
	long answer = 0;

	for (count = 0; count < 10; count++)
	{
		puts("Enter an integer: ");
		scanf("%d", &temp);
		answer += temp;
	}

	printf("The sum of all the number is %ld\n", answer);

	return 0;
}
