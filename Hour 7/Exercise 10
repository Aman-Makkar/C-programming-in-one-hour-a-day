#include <stdio.h>

int main (void)
{
	float a, b;

	puts("Enter two floating point numbers and I will show you their sum!");
	scanf("%f %f", &a, &b);

	printf("The sum of %.2f and %.2f is %.2f\n", a, b, a*b);

	return 0;
}
